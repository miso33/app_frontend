export const Options = (title) => {
    return {
        addRowPosition: "first",
        columnsButton: true,
        filtering: true,
        pageSize: 10,
        search: false,
        emptyRowsWhenPaging: false,
        tableLayout: 'auto',
    }
}
