import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles({

    fab: {
        position: "relative",
        bottom: 0,
        left: 0,
        right: 0,

    },

});